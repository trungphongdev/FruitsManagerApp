<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qunlcahnghoaqu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/shop"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/groupfruit"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".About_Infor" />
        <activity android:name=".CalcData" />
        <activity
            android:name=".sign_in_layout"
            android:theme="@style/AppTheme2" />
        <activity android:name=".ListFood"
            android:theme="@style/AppTheme2"/>
        <activity android:name=".AddFruits"
            android:theme="@style/AppTheme2" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>